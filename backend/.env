###> symfony/framework-bundle ###
APP_ENV=dev
APP_SECRET=ChangeMe123456789
APP_DEBUG=1
###< symfony/framework-bundle ###

###> doctrine/doctrine-bundle ###
# Importante: host = db porque así se llama el servicio en docker-compose
DATABASE_URL="mysql://user:pass@db:3306/controlvehicular?serverVersion=8.0&charset=utf8mb4"
###< doctrine/doctrine-bundle ###

###> symfony/mailer ###
# Configuración temporal (SMTP local / sin autenticación)
# Reemplaza esto con la configuración real cuando la tengas:
MAILER_DSN=smtp://localhost:1025
DEFAULT_URI=http://localhost
