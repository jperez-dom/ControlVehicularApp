<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Reporte de Comisión - {{ comission.folio }}</title>
    <style>
        @page { margin: 25px; }
        body {
            font-family: 'Helvetica', 'Arial', sans-serif;
            color: #333;
            font-size: 12px;
            line-height: 1.4;
        }
        .container {
            width: 100%;
            margin: 0 auto;
        }
        .header {
            text-align: center;
            padding-bottom: 15px;
            border-bottom: 3px solid #000;
        }
        .logo-container {
            background-color: #000;
            padding: 10px;
            display: inline-block;
            border-radius: 8px;
        }
        .logo {
            max-height: 50px;
            width: auto;
        }
        .report-main-title {
            text-align: center;
            margin-top: 20px;
            margin-bottom: 15px;
        }
        .report-title {
            font-size: 24px;
            font-weight: bold;
            color: #333;
            margin: 0;
        }
        .folio {
            font-size: 14px;
            color: #555;
        }
        .section {
            margin-top: 25px;
            page-break-inside: avoid;
        }
        .section-title {
            font-size: 16px;
            font-weight: bold;
            color: #333;
            border-bottom: 1px solid #ccc;
            padding-bottom: 5px;
            margin-bottom: 10px;
        }
        .subsection-title {
            font-size: 14px;
            font-weight: bold;
            color: #555;
            margin-top: 15px;
            margin-bottom: 5px;
        }
        .main-table {
            width: 100%;
            border-collapse: collapse;
        }
        .main-table th, .main-table td {
            padding: 8px 10px;
            border: 1px solid #ddd;
            text-align: left;
        }
        .main-table th {
            background-color: #f2f2f2;
            font-weight: bold;
        }
        .info-table th { width: 25%; }
        .ruta-table th:first-child { width: 10%; }

        .photo-table td {
            width: 33.33%;
            padding: 5px;
            text-align: center;
            vertical-align: top;
            border: none;
        }
        .photo-box {
            border: 1px solid #ddd;
            background-color: #fafafa;
            padding: 8px;
            height: 200px;
            box-sizing: border-box;
            text-align: center;
        }
        .photo-box img {
            max-width: 100%;
            max-height: 160px;
            display: block;
            margin: 0 auto;
        }
        .photo-label {
            font-size: 10px;
            margin-top: 5px;
            color: #555;
            font-weight: bold;
        }

        .signature-table td {
            width: 50%;
            padding: 10px;
            border: none;
        }
        .signature-content {
            border: 1px solid #ccc;
            background-color: #f9f9f9;
            height: 150px;
            padding: 10px;
            text-align: center;
        }
        .signature-content img {
            max-height: 100px;
            max-width: 100%;
        }
        .signature-label {
            margin-top: 8px;
            font-weight: bold;
            border-top: 1px solid #aaa;
            padding-top: 8px;
            text-align: center;
        }

        .footer {
            position: fixed;
            bottom: -25px;
            left: 0;
            right: 0;
            height: 20px;
            text-align: center;
            font-size: 10px;
            color: #888;
            border-top: 1px solid #eee;
            padding-top: 5px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            {% if logoPath %}
                <div class="logo-container">
                    <img class="logo" src="{{ logoPath }}" alt="Logo">
                </div>
            {% endif %}
        </div>

        <div class="report-main-title">
            <h1 class="report-title">Reporte de Comisión</h1>
            <div class="folio">Folio: <strong>{{ comission.folio }}</strong></div>
        </div>

        <div class="section">
            <h2 class="section-title">Datos Generales</h2>
            <table class="main-table info-table">
                <tr>
                    <th>Conductor</th>
                    <td>{{ comission.driverName | default('N/A') }} ({{ comission.driverPosition | default('N/A') }})</td>
                    <th>Vehículo</th>
                    <td>{{ comission.vehicleName | default('N/A') }} (Placa: {{ comission.vehiclePlate | default('N/A') }})</td>
                </tr>
                <tr>
                    <th>Estado Actual</th>
                    <td>{{ comission.status }}</td>
                    <th>Fecha de Generación</th>
                    <td>{{ "now"|date('d/m/Y H:i') }}</td>
                </tr>
            </table>
        </div>

        <div class="section">
            <h2 class="section-title">Detalles del Recorrido</h2>
            <table class="main-table">
                <tr>
                    <th>Fecha/Hora Salida</th>
                    <td>{{ pass.startDate ? pass.startDate|date('d/m/Y H:i') : '—' }}</td>
                    <th>Fecha/Hora Entrada</th>
                    <td>{{ pass.endDate ? pass.endDate|date('d/m/Y H:i') : '—' }}</td>
                </tr>
                <tr>
                    <th>Kilometraje Salida</th>
                    <td>{{ pass.departureMileage ? pass.departureMileage | number_format(0, '.', ',') ~ ' km' : '—' }}</td>
                    <th>Kilometraje Entrada</th>
                    <td>{{ pass.arrivalMileage ? pass.arrivalMileage | number_format(0, '.', ',') ~ ' km' : '—' }}</td>
                </tr>
                <tr>
                    <th>Combustible Salida</th>
                    <td>{{ pass.departureFuel ? pass.departureFuel ~ '/8' : '—' }}</td>
                    <th>Combustible Entrada</th>
                    <td>{{ pass.arrivalFuel ? pass.arrivalFuel ~ '/8' : '—' }}</td>
                </tr>
                 <tr>
                    <th>Observaciones Salida</th>
                    <td colspan="3">{{ pass.comment_salida | default('Sin observaciones.') }}</td>
                </tr>
                <tr>
                    <th>Observaciones Entrada</th>
                    <td colspan="3">{{ pass.comment_entrada | default('Sin observaciones.') }}</td>
                </tr>
            </table>
        </div>

        <div class="section">
            <h2 class="section-title">Ruta de la Comisión</h2>
            <table class="main-table ruta-table">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Estado</th>
                        <th>Ciudad</th>
                        <th>Comentario</th>
                    </tr>
                </thead>
                <tbody>
                {% for d in destinos %}
                    <tr>
                        <td>{{ loop.index }}</td>
                        <td>{{ d.estado }}</td>
                        <td>{{ d.ciudad }}</td>
                        <td>{{ d.comentario | default('—') }}</td>
                    </tr>
                {% else %}
                    <tr><td colspan="4" style="text-align: center;">No se registraron destinos.</td></tr>
                {% endfor %}
                </tbody>
            </table>
        </div>

        <div class="section">
            <h2 class="section-title">Evidencia Fotográfica</h2>

            <h3 class="subsection-title">Salida</h3>
            {% if photos.salida is not empty %}
                <table class="photo-table">
                    {% for row in photos.salida|batch(3) %}
                        <tr>
                        {% for photo in row %}
                            <td>
                                <div class="photo-box">
                                    {% if photo.path %}<img src="{{ photo.path }}" alt="{{ photo.label }}">{% else %}<span>Sin imagen</span>{% endif %}
                                </div>
                                <div class="photo-label">{{ photo.label }}</div>
                            </td>
                        {% endfor %}
                        {% if row|length < 3 %}
                            {% for i in 1..(3 - row|length) %}<td></td>{% endfor %}
                        {% endif %}
                        </tr>
                    {% endfor %}
                </table>
            {% else %}
                <p style="text-align: center;">No hay fotografías de salida.</p>
            {% endif %}

            <h3 class="subsection-title">Entrada</h3>
            {% if photos.entrada is not empty %}
                <table class="photo-table">
                    {% for row in photos.entrada|batch(3) %}
                        <tr>
                        {% for photo in row %}
                            <td>
                                <div class="photo-box">
                                    {% if photo.path %}<img src="{{ photo.path }}" alt="{{ photo.label }}">{% else %}<span>Sin imagen</span>{% endif %}
                                </div>
                                <div class="photo-label">{{ photo.label }}</div>
                            </td>
                        {% endfor %}
                        {% if row|length < 3 %}
                            {% for i in 1..(3 - row|length) %}<td></td>{% endfor %}
                        {% endif %}
                        </tr>
                    {% endfor %}
                </table>
            {% else %}
                <p style="text-align: center;">No hay fotografías de entrada.</p>
            {% endif %}
        </div>

        <div class="section">
            <h2 class="section-title">Firmas de Conformidad</h2>
            <table class="signature-table">
                <tr>
                    <td>
                        <div class="signature-content">
                            {% if signatures.driver_departure %}<img src="{{ signatures.driver_departure }}" alt="Firma Conductor Salida">{% endif %}
                        </div>
                        <div class="signature-label">Firma ({{ comission.driverName | default('N/A') }}) (Salida)</div>
                    </td>
                    <td>
                        <div class="signature-content">
                            {% if signatures.approver_departure %}<img src="{{ signatures.approver_departure }}" alt="Firma Aprobador Salida">{% endif %}
                        </div>
                        <div class="signature-label">Firma ({{ comission.approverName | default('N/A') }}) (Salida)</div>
                    </td>
                </tr>
                <tr>
                    <td>
                        <div class="signature-content">
                            {% if signatures.driver_arrival %}<img src="{{ signatures.driver_arrival }}" alt="Firma Conductor Entrada">{% endif %}
                        </div>
                        <div class="signature-label">Firma ({{ comission.driverName | default('N/A') }}) (Entrada)</div>
                    </td>
                    <td>
                        <div class="signature-content">
                            {% if signatures.approver_arrival %}<img src="{{ signatures.approver_arrival }}" alt="Firma Aprobador Entrada">{% endif %}
                        </div>
                        <div class="signature-label">Firma ({{ comission.approverName | default('N/A') }}) (Entrada)</div>
                    </td>
                </tr>
            </table>
        </div>

        <div class="footer">
            Reporte generado automáticamente por Control Vehicular App &copy; {{ "now"|date("Y") }}
        </div>
    </div>
</body>
</html>
